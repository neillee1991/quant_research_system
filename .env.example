# ===== 量化研究系统 环境变量配置 =====
# 复制此文件为 .env 并填入实际值
# 支持两种命名方式：
#   扁平: TUSHARE_TOKEN=xxx
#   嵌套: COLLECTOR__TUSHARE_TOKEN=xxx (对应 settings.collector.tushare_token)

# Tushare API Token（必填，用于数据同步）
TUSHARE_TOKEN=your_tushare_token_here

# PostgreSQL 数据库
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=quant_research
POSTGRES_USER=quant_user
POSTGRES_PASSWORD=your_password_here

# 连接池
DATABASE__CONNECTION_POOL_SIZE=10

# API 限流
COLLECTOR__CALLS_PER_MINUTE=120

# 回测配置
BACKTEST__INITIAL_CAPITAL=1000000
BACKTEST__COMMISSION_RATE=0.0003

# 应用配置
ENVIRONMENT=development
DEBUG=false
