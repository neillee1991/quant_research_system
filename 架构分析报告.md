# 量化研究系统 - 完整架构分析报告

> 版本：v2.1.0 | 分析日期：2026-04-25 | 分析团队：UI、前端、后端、数据库、量化、系统架构
>
> **说明**：报告中使用 ✅ 标注已有完整代码实现的功能，❌ 标注历史遗留/规划中但当前无实际代码的功能。

---

## 目录

1. [项目概览](#1-项目概览)
2. [系统架构总图](#2-系统架构总图)
3. [技术栈全景](#3-技术栈全景)
4. [后端架构](#4-后端架构)
5. [前端架构](#5-前端架构)
6. [数据库架构](#6-数据库架构)
7. [量化引擎](#7-量化引擎)
8. [调度系统](#8-调度系统)
9. [安全机制](#9-安全机制)
10. [监控体系](#10-监控体系)
11. [基础设施](#11-基础设施)
12. [问题与建议](#12-问题与建议)

---

## 1. 项目概览

**量化研究系统（Quant Research System）** 是一个面向量化交易研究的全栈平台，集成了数据采集、因子计算、因子分析和调度编排能力。

### 1.1 核心能力

| 能力域 | 状态 | 描述 |
|--------|------|------|
| 数据管理 | ✅ 已实现 | Tushare 数据采集、增量同步、ETL 转换、双库存储 |
| 因子工程 | ✅ 已实现 | 数据库驱动因子、增量/全量计算、8 阶段处理管道 |
| 因子分析 | ✅ 已实现 | IC/IR 分析（Alphalens）、分层收益、换手率 |
| 调度编排 | ✅ 已实现 | 自研 DAG 调度器（最小堆 + Cron），替代 Prefect |
| 配置管理 | ✅ 已实现 | 配置导入导出、差异比较、指数订阅 |
| 可视化 | ✅ 已实现 | ECharts 图表、K 线图、React Flow DAG 编辑器 |
| 回测引擎 | ❌ 已删除 | 原 VectorBT 封装已删除，待从 0 重构 |
| 策略建模 | ❌ 未实现 | 策略 DSL 解析器目录不存在，strategy API 无实现 |
| AutoML | ❌ 未实现 | 无任何业务代码 |
| ML 权重 | ❌ 未实现 | 前端 ml.ts 为占位符，后端无对应路由 |

### 1.2 代码规模

| 类别 | 数量 |
|------|------|
| Python 文件 | 141 个 |
| TypeScript/JavaScript 文件 | 95 个 |
| 测试文件 | 17 个 |
| 数据库建表脚本 | 1 个（create_tables.sql） |
| YAML 流程配置 | 2 个 |

---

## 2. 系统架构总图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户浏览器                                   │
│              React 18 + TypeScript + Ant Design                     │
│   DataCenter | FactorCenter | ConfigCenter | MarketCenter           │
└──────────────────────────┬──────────────────────────────────────────┘
                           │ HTTP/REST (axios)
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    FastAPI 后端 (Python 3.11)                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    API 层 /api/v1/                           │   │
│  │  /data  /factor  /tasks  /flows  /config                    │   │
│  └──────────────────────────┬──────────────────────────────────┘   │
│                             │                                        │
│  ┌──────────────────────────▼──────────────────────────────────┐   │
│  │                    服务层 (Services)                          │   │
│  │  TaskService | FlowService | FactorService                   │   │
│  └──────────────────────────┬──────────────────────────────────┘   │
│                             │                                        │
│  ┌──────────┬───────────────▼──────────────┬──────────────────┐   │
│  │ 调度器   │       量化引擎               │   数据管理器      │   │
│  │Scheduler │  FactorEngine | Analysis     │  SyncEngine      │   │
│  └──────────┴──────────────────────────────┴──────────────────┘   │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
          ┌────────────────┴────────────────┐
          ▼                                 ▼
┌─────────────────┐               ┌─────────────────┐
│   DolphinDB     │               │   PostgreSQL 16  │
│  (时序数据库)    │               │  (元数据/配置)   │
│                 │               │                  │
│ daily_data      │               │ flow_configs     │
│ daily_basic     │               │ flow_runs        │
│ adj_factor      │               │ task_runs        │
│ index_daily     │               │ sync_task_configs│
│ moneyflow       │               │ etl_task_configs │
│ factor_values   │               │ factor_configs   │
│ stock_basic     │               │ factor_analysis  │
│ trade_cal       │               │ _results         │
└─────────────────┘               └─────────────────┘
```

---

## 3. 技术栈全景

### 3.1 后端技术栈

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| Web 框架 | FastAPI | >=0.111.0 | 异步 REST API |
| 数据处理 | Polars | >=1.0.0 | 向量化数据操作 |
| 数据处理 | Pandas | >=2.1.0 | 兼容层（Alphalens 依赖） |
| 时序数据库 | DolphinDB | >=3.0.0.0 | 行情数据、因子值存储 |
| 关系数据库 | PostgreSQL | 16 | 配置、调度、元数据 |
| 异步驱动 | asyncpg | >=0.29.0 | PostgreSQL 异步连接池 |
| 同步驱动 | psycopg2-binary | >=2.9.0 | 因子加载同步查询 |
| 因子分析 | alphalens-reloaded | >=0.4.3 | IC/IR 分析 |
| 数据源 | Tushare | >=1.4.19 | A 股行情数据 |
| 调度 | 自研调度器 | - | DAG + Cron，替代 Prefect |
| Cron 解析 | croniter | >=2.0.0 | Cron 表达式解析 |
| 监控 | prometheus-client | >=0.20.0 | Prometheus 指标暴露 |
| 配置 | pydantic-settings | >=2.3.0 | 分层配置管理 |
| 日志 | loguru | >=0.7.0 | 结构化日志 |

### 3.2 前端技术栈

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| UI 框架 | React | 18 | 组件化 UI |
| 类型系统 | TypeScript | latest | 类型安全 |
| UI 组件库 | Ant Design | 6.3.3 | 企业级组件 |
| 图表库 | ECharts | 5.5.0 | 数据可视化 |
| K 线图 | lightweight-charts | 5.1.0 | 金融图表 |
| 流程图 | React Flow | 11.11.4 | DAG 可视化编辑 |
| 状态管理 | Zustand | 4.5.4 | 轻量全局状态 |
| HTTP 客户端 | Axios | 1.7.2 | API 调用 |
| 路由 | React Router | 6.24.0 | SPA 路由 |
| 类型生成 | openapi-typescript | 6.7.6 | 从 OpenAPI 生成类型 |

### 3.3 基础设施

| 组件 | 技术 | 用途 |
|------|------|------|
| 容器化 | Docker + Docker Compose | 服务编排 |
| 网络 | bridge 网络 quant_network | 容器间通信 |
| 数据持久化 | 本地卷挂载 | 数据库数据持久化 |
| 健康检查 | Docker healthcheck | 服务可用性监控 |

---

## 4. 后端架构

### 4.1 目录结构

```
backend/
├── app/                          # FastAPI 应用核心
│   ├── main.py                   # 应用入口、路由注册、生命周期
│   ├── api/v1/                   # API 路由层
│   │   ├── data/                 # ✅ 数据查询 API
│   │   ├── factor/               # ✅ 因子管理 API（registry/compute/analysis）
│   │   ├── tasks/                # ✅ 任务管理 API（crud/etl/execute/monitor）
│   │   ├── flows.py              # ✅ 流程编排 API
│   │   └── config/               # ✅ 配置管理 API
│   ├── core/                     # 核心模块
│   │   ├── config.py             # ✅ 配置管理（分层配置类）
│   │   ├── container.py          # ✅ 依赖注入容器（单例）
│   │   ├── interfaces.py         # ✅ 核心接口定义
│   │   ├── enums.py              # ✅ 枚举类型
│   │   ├── constants.py          # ✅ 系统常量
│   │   ├── sandbox.py            # ✅ 沙箱安全执行
│   │   ├── exceptions.py         # ✅ 自定义异常
│   │   └── logger.py             # ✅ 日志配置
│   ├── models/                   # ✅ Pydantic 数据模型
│   ├── services/                 # ✅ 业务逻辑层（9 个服务）
│   └── validators/               # ✅ 输入验证器
├── engine/                       # 量化引擎
│   ├── factor/                   # ✅ 因子注册与计算
│   │   ├── registry.py           # ✅ 数据库驱动因子注册表
│   │   └── data_config.py        # ✅ 因子数据配置加载器
│   ├── analysis/                 # ✅ 因子分析（6 个文件）
│   │   ├── analyzer.py           # ✅ 分析核心
│   │   ├── alphalens_adapter.py  # ✅ Alphalens 适配器
│   │   ├── forward_returns.py    # ✅ 前向收益计算
│   │   ├── neutralizer.py        # ✅ 中性化处理
│   │   ├── diagnostics.py        # ✅ 诊断工具
│   │   └── data_cache.py         # ✅ 数据缓存
│   └── parser/                   # ❌ 不存在，策略 DSL 未实现
├── data_manager/                 # ✅ 数据同步引擎
│   ├── refactored_sync_engine.py # ✅ 同步引擎主体
│   ├── sync_components.py        # ✅ 同步组件
│   ├── processor.py              # ✅ 数据处理器
│   ├── schema_generator.py       # ✅ Schema 生成器
│   └── collectors/
│       └── tushare_collector.py  # ✅ Tushare 采集器
├── scheduler/                    # ✅ 任务调度系统
│   ├── core.py                   # ✅ 调度器主循环
│   ├── executor.py               # ✅ DAG 执行器
│   ├── repository.py             # ✅ 数据库仓储层
│   ├── submitter.py              # ✅ 任务提交器
│   ├── models.py                 # ✅ 调度数据模型
│   └── db.py                     # ✅ PostgreSQL 连接池
├── infrastructure/               # 基础设施
│   ├── database/                 # ✅ DolphinDB 客户端（8 个模块）
│   ├── monitoring/               # ✅ 性能监控（2 个模块）
│   └── processor/                # ✅ 数据处理管道（3 个模块）
└── database/                     # 数据库初始化
    ├── create_tables.sql              # ✅ PostgreSQL 完整建表脚本（新列名，无 _json 后缀）
    ├── migrate_rename_json_columns.sql # ✅ 存量数据库列名迁移脚本
    ├── init_postgres.py               # ✅ 执行建表脚本
    ├── init_dolphindb.py              # ✅ DolphinDB 初始化
    └── init_meta_tables.py            # ✅ 元数据导入
```

### 4.2 应用启动流程

```
FastAPI lifespan startup
  1. 初始化 PostgreSQL 连接池 (asyncpg)
  2. 启动调度器 (Scheduler)
  3. 创建 DolphinDB 维度表 (ensure_meta_tables)
  4. 预加载因子字段映射缓存
  5. 清理僵尸任务 (zombie task cleanup)

FastAPI lifespan shutdown
  1. 停止调度器
  2. 关闭 PostgreSQL 连接池
```

### 4.3 API 路由结构

```
/api/v1/
├── /data/
│   ├── GET  /stocks              - 股票列表（查 DolphinDB stock_basic）
│   ├── GET  /daily               - 日线数据
│   └── POST /query               - 自定义 SQL 查询
├── /factor/
│   ├── GET  /factors             - 因子列表
│   ├── POST /factors             - 创建因子
│   ├── PUT  /factors/{id}        - 更新因子
│   ├── DELETE /factors/{id}      - 删除因子
│   ├── GET  /factors/{id}/code   - 获取因子代码
│   ├── PUT  /factors/{id}/code   - 更新因子代码
│   ├── GET  /factors/{id}/data   - 查询因子值
│   ├── GET  /factors/{id}/stats  - 因子统计
│   ├── POST /run                 - 运行因子计算
│   ├── POST /batch-run           - 批量运行
│   ├── POST /factors/test        - 测试因子代码
│   └── POST /analysis/alphalens  - Alphalens 分析
├── /tasks/
│   ├── GET  /running             - 运行中任务
│   ├── GET  /history             - 任务历史
│   ├── GET  /sync                - 同步任务列表
│   ├── POST /sync/{id}/execute   - 执行同步任务
│   ├── GET  /etl                 - ETL 任务列表
│   └── POST /etl/{id}/execute    - 执行 ETL 任务
├── /flows/
│   ├── GET  /                    - 流程列表
│   ├── POST /                    - 创建流程
│   ├── POST /{name}/trigger      - 触发流程
│   ├── POST /{name}/backfill     - 回溯执行
│   ├── GET  /{name}/runs         - 流程运行历史
│   └── GET  /{name}/runs/{id}    - 运行详情
└── /config/
    ├── /field_mappings           - 字段映射管理
    ├── /import_export            - 配置导入导出
    └── /index_subscribe          - 指数订阅
```

### 4.4 依赖注入设计

采用单例容器模式，避免重复初始化：

```python
class Container:  # 单例
    get_factor_service()   → FactorService
    get_sync_engine()      → RefactoredSyncEngine
```

FastAPI 通过 `Depends(get_factor_service)` 注入到路由处理函数。

### 4.5 核心接口定义

| 接口 | 核心方法 | 实现类 | 状态 |
|------|---------|--------|------|
| IDataCollector | collect_daily, collect_stock_basic | TushareCollector | ✅ |
| IDataRepository | query, upsert, table_exists, create_table | DolphinDBRepository | ✅ |
| IFactorEngine | compute_technical_factors, compute_cross_sectional_factors | FactorEngine | ✅ |
| ISyncTaskExecutor | execute_task | SyncTaskExecutor | ✅ |
| IRateLimiter | wait, reset | TokenBucketRateLimiter | ✅ |
| IRetryPolicy | execute | ExponentialBackoffRetry | ✅ |
| IBacktestEngine | run_backtest | - | ❌ 已删除，待从 0 重构 |

### 4.6 服务层设计

**泛型 TaskService[T]** 统一管理三类任务配置：

```python
sync_service   = TaskService[SyncTaskConfig](table="sync_task_configs")
etl_service    = TaskService[ETLTaskConfig](table="etl_task_configs")
factor_service = TaskService[FactorConfig](table="factor_configs")
```

特性：软删除、共享表保护、Schema 演变验证。

**FlowService** 管理 DAG 流程配置，支持版本控制（每次更新递增版本号）。

---

## 5. 前端架构

### 5.1 页面路由结构

```
App.tsx (BrowserRouter + ErrorBoundary)
├── /              → DataCenter    数据中心
├── /market        → MarketCenter  市场中心
├── /factor        → FactorCenter  因子中心
├── /config        → ConfigCenter  配置中心
└── *              → redirect /
```

### 5.2 页面组件结构

```
pages/
├── DataCenter/
│   ├── index.tsx              - ✅ 主页面（Tab 切换）
│   ├── SyncPanel.tsx          - ✅ 同步任务管理
│   ├── ETLPanel.tsx           - ✅ ETL 任务管理
│   ├── SchedulerPanel.tsx     - ✅ 调度配置（Flow DAG）
│   ├── QuickSubscribeModal.tsx - ✅ 快速订阅指数
│   └── Modals.tsx             - ✅ 任务配置模态框
├── FactorCenter/
│   ├── index.tsx              - ✅ 因子中心主页
│   ├── FactorManageTab.tsx    - ✅ 因子列表管理
│   ├── FactorDrawer.tsx       - ✅ 因子详情/编辑抽屉
│   ├── FactorFlowDrawer.tsx   - ✅ 因子流程抽屉
│   ├── FactorTaskDrawer.tsx   - ✅ 因子任务抽屉
│   └── TestPanel.tsx          - ✅ 因子测试面板
├── ConfigCenter/
│   ├── index.tsx              - ✅ 配置中心主页
│   ├── FieldMappingsTab.tsx   - ✅ 字段映射配置
│   ├── IndexSubscribeTab.tsx  - ✅ 指数订阅管理
│   ├── ImportExportTab.tsx    - ✅ 配置导入导出
│   ├── DiffViewer.tsx         - ✅ 配置差异查看器
│   └── ImportResult.tsx       - ✅ 导入结果展示
├── MarketCenter.tsx           - ✅ 市场行情中心
└── UnifiedTaskCenter.tsx      - ✅ 统一任务中心
```

### 5.3 状态管理（Zustand）

4 个独立 Store，无跨 Store 依赖：

| Store | 状态 | 用途 |
|-------|------|------|
| useThemeStore | mode: dark/light | 主题切换，持久化到 localStorage |
| useTaskMonitorStore | runningTasks, isLoading | 全局任务监控轮询 |
| useNavStore | dataTab | 数据中心标签页状态 |
| useStrategyScriptStore | code, runStatus, compileResult | 策略脚本执行状态机 |

### 5.4 API 调用层

```
src/api/
├── client.ts      - ✅ axios 实例（普通 60s + 长任务 300s 两个实例）
├── data.ts        - ✅ 数据管理 API（同步/ETL/查询）
├── factor.ts      - ✅ 因子管理 API（CRUD + 计算 + 分析）
├── flow.ts        - ✅ 流程编排 API
├── monitor.ts     - ✅ 任务监控 API
├── config.ts      - ✅ 配置管理 API（指数订阅 + 导入导出）
└── ml.ts          - ❌ 占位符，后端无对应实现
```

设计特点：普通请求 timeout=60s，因子计算/Alphalens 等耗时操作使用 longRunningApi（timeout=300s）。统一 ApiResponse<T> 响应信封：`{ success, data, message, error }`。

### 5.5 类型定义体系

```
src/types/
├── generated.ts      - 从后端 OpenAPI 自动生成（禁止手动编辑）
├── api.ts            - ApiResponse<T>、PaginatedResponse<T>、StatusInfo
├── task.ts           - ✅ SyncTaskConfig、ETLTaskConfig、FactorConfig
├── factor.ts         - ✅ FactorDefinition、PreprocessOptions
├── strategy.ts       - ✅ ScriptRunStatus（已移除回测类型）
├── data.ts           - ✅ SyncTask、ETLTask、DailyData、StockInfo
├── stockPool.ts      - ✅ 股票池类型
└── indexSubscribe.ts - ✅ 指数订阅类型
```

**类型同步策略**：后端 Pydantic 模型 → OpenAPI Schema → `generated.ts`（自动生成），手写类型文件处理前端特定需求。

### 5.6 JSON 字段处理约定

后端 JSONB 字段直接返回 dict/list，前端通过 `parseJsonFields` 函数创建运行时别名：
- `params_json` → `params`
- `schema_json` → `schema`
- `primary_keys_json` → `primary_keys`

---

## 6. 数据库架构

### 6.1 双数据库职责划分

| 数据库 | 类型 | 职责 | 数据特征 |
|--------|------|------|---------|
| DolphinDB v3.00.5 | 时序数据库 | 行情数据、因子值 | 高频写入、时序查询 |
| PostgreSQL 16 | 关系数据库 | 配置、调度、元数据 | 低频写入、复杂查询 |

### 6.2 DolphinDB 表结构

**TSDB 分区表**（按 trade_date 分区）：

| 表名 | 主要字段 | 分区策略 |
|------|---------|---------|
| sync_daily_data | ts_code, trade_date, open/high/low/close/vol | RANGE(trade_date, 季度) + HASH(ts_code) |
| sync_daily_basic | ts_code, trade_date, pe/pb/turnover_rate | RANGE(trade_date, 季度) + HASH(ts_code) |
| sync_adj_factor | ts_code, trade_date, adj_factor | RANGE(trade_date, 季度) + HASH(ts_code) |
| sync_index_daily | ts_code, trade_date, OHLCV | RANGE(trade_date, 季度) |
| sync_moneyflow | ts_code, trade_date, 资金流向字段 | RANGE(trade_date, 季度) |
| factor_values | ts_code, factor_id, trade_date, factor_value, quality_flag | HASH(factor_id,20) + RANGE(trade_date,季度) + HASH(ts_code,10) |

**维度表**（Dimension Table）：

| 表名 | 内容 |
|------|------|
| stock_basic | 股票基础信息（代码、名称、行业、上市日期） |
| trade_cal | 交易日历（交易所、日期、是否交易日） |
| sync_log | 同步日志 |
| factor_metadata | 因子元数据 |

**factor_values 质量标记位掩码**：

```
QUALITY_NORMAL          = 0   正常
QUALITY_LIMIT_UP        = 1   一字涨停
QUALITY_LIMIT_DOWN      = 2   一字跌停
QUALITY_POST_SUSPENSION = 4   停牌复牌窗口期
QUALITY_LOW_VOLUME      = 8   极低成交量
QUALITY_IPO_PERIOD      = 16  新股上市初期
```

### 6.3 PostgreSQL 表结构

**调度系统表**：

```sql
flow_configs          -- DAG 流程定义（含 cron、tasks JSONB、version）
flow_runs             -- 流程执行记录（status、trigger_type、target_date、run_id）
task_runs             -- 任务执行记录（run_id、rows、elapsed_sec、error）
```

**任务配置表**：

```sql
sync_task_configs     -- 数据同步任务（api_name、params_json JSONB、schema_json JSONB）
etl_task_configs      -- ETL 任务（script、schema_json、primary_keys_json）
factor_configs        -- 因子配置（code、compute_mode、depends_on、params）
factor_field_mappings -- 因子字段映射（field_key → table_name.column_name）
```

**参考数据表**：

```sql
index_configs         -- 指数配置（index_code、stock_count、latest_date）
user_preferences      -- 用户偏好（index_table、filter_config）
```

**结果表**：

```sql
factor_analysis_results  -- Alphalens 分析结果（ic_mean、ic_ir、report_path）
```

**版本追踪表**：

```sql
schema_migrations     -- 记录已执行的 schema 版本
```

### 6.4 数据库初始化策略

单一建表脚本，幂等（`IF NOT EXISTS`），全新部署直接执行：

```
database/create_tables.sql   -- 完整 schema，包含所有表、索引、触发器
database/init_postgres.py    -- 执行 create_tables.sql，验证表是否存在
```

`start.sh` 每次启动时自动调用 `init_postgres.py`，已存在的表不受影响。

### 6.5 DolphinDB 客户端架构

门面模式（Facade），内部由 8 个专职模块组成：

```
DolphinDBClient (门面)
├── DolphinDBConnection  连接管理（单例 + 线程锁 + 自动重连）
├── SQLAdapter           SQL 语法适配（PostgreSQL → DolphinDB）
├── TypeConverter        类型转换（Python ↔ DolphinDB）
├── TableManager         表操作（建表、检查存在、删除）
├── DataOperations       数据操作（query、upsert、append、bulk_copy）
├── MetadataManager      元数据管理（维度表维护）
├── QueryBuilder         查询构建器
└── DolphinDBRepository  Repository 接口实现
```

**SQL 适配规则**：
- `%s` 占位符 → 实际值替换
- 裸表名 → `loadTable("dfs://quant", "table_name")`
- `CURRENT_TIMESTAMP` → `now()`
- 移除不支持的 `RETURNING` 子句

**Upsert 策略**：
- 全量同步：清空整表 → 写入
- 增量同步（有 trade_date）：清空指定日期 + factor_id → 写入
- 默认：按 key_columns 删除已存在行 → 插入

---

## 7. 量化引擎

### 7.1 因子引擎架构

#### 因子注册机制

因子完全由数据库驱动，代码存储在 PostgreSQL `factor_configs.code` 字段中，运行时动态加载：

```python
# 因子代码示例（存储在数据库中）
def compute_ma_20(df: pl.DataFrame, params: dict) -> pl.DataFrame:
    return df.with_columns(...)
```

**加载流程**：
1. 从 PostgreSQL `factor_configs` 查询 `code` 字段不为空的因子
2. 通过沙箱安全检查（AST 静态分析）
3. `exec(code, namespace)` 动态编译，提取名称以 `compute` 开头或名为 `main` 的函数
4. 注册到内存 `_factor_registry`，5 分钟缓存，支持 force_refresh

**FactorDefinition 结构**：

| 字段 | 类型 | 说明 |
|------|------|------|
| factor_id | str | 唯一标识 |
| depends_on | List[str] | 数据源依赖（sync_daily_data / factor_xxx） |
| category | str | 分类（technical/momentum/value/quality/volatility） |
| compute_mode | str | incremental（增量）/ full（全量） |
| storage | StorageConfig | 存储目标（factor_values 或自定义表） |
| align_calendar | bool | 是否对齐交易日历 |

#### 因子计算流程

```
FactorComputeService.compute_factor(factor_id, target_date, options)
  ├── discover_factors()          从数据库加载因子定义（5分钟缓存）
  ├── get_factor(factor_id)       从注册表获取因子定义
  ├── _resolve_preprocess_options() 合并预处理配置（4层优先级）
  ├── _resolve_dates()            计算计算日期范围
  ├── ProcessContext 创建         初始化处理上下文
  ├── PipelineFactory.create_factor_pipeline() 构建处理管道
  └── pipeline.execute(context)  执行管道
```

**预处理配置优先级**（低 → 高）：
1. 系统默认配置（DEFAULT_PREPROCESS）
2. 因子定义中的 `params.preprocess`
3. 数据库 `factor_configs` 中的 `params`
4. 显式传入的 `preprocess` 参数

#### 数据处理管道（Pipeline）

8 个有序阶段，每阶段可按条件跳过：

```
1. DataLoaderProcessor      从数据库加载原始数据
2. AdjustmentProcessor      复权处理（forward/backward/none）
3. StatusFilterProcessor    ST 过滤、新股过滤、涨跌停标记
4. FactorComputeProcessor   执行因子计算函数（Polars 向量化）
5. CalendarAlignProcessor   交易日历对齐（align_calendar=True 时启用）
6. DateRangeFilterProcessor 按 calc_start/calc_end 过滤
7. QualityCheckerProcessor  质量检查，生成 quality_flag 位掩码
8. ResultWriterProcessor    保存到 factor_values 表
```

#### 数据预处理细节

**停牌处理**：
- 检测交易日 gap（`_suspension_gap` 列）
- 复牌后 window 行的 factor_value 置为 null
- 设置 `QUALITY_POST_SUSPENSION` 质量标记

**一字涨跌停判断**：
- 条件1：`|pct_chg| ≥ 9.8%` 且 `vol < vol_threshold`
- 条件2：`open == close == high == low`（一字板）
- 结果：1=涨停，-1=跌停，0=正常

### 7.2 回测引擎

❌ **已删除，待重构**

原 VectorBT 封装（`engine/backtester/`）已完整删除，包括：
- `VectorEngine`、`BacktestConfig`、`BacktestResult`
- `BacktestService`、`IBacktestEngine` 接口
- 相关 PostgreSQL 表及迁移脚本
- 前端 `BacktestMetrics`、`BacktestResult` 等类型定义

回测引擎计划从 0 开始重构，设计方案待定。

### 7.3 因子分析（Alphalens）

**分析流程**：
1. 从 DolphinDB 加载因子值和价格数据
2. 调用 Alphalens 计算 IC、Rank IC、分层收益
3. 结果保存到 PostgreSQL `factor_analysis_results` 表
4. 支持异步执行（长任务，timeout=300s）

**关键指标**：ic_mean、ic_std、rank_ic_mean、rank_ic_std、ic_ir、turnover_mean

---

## 8. 调度系统

### 8.1 调度器架构

自研轻量级调度器，替代 Prefect，核心组件：

```
Scheduler
├── _heap: List[ScheduledFlow]   最小堆（按 next_run 排序）
├── _flow_versions: Dict         版本跟踪（检测配置变更）
├── _executor: DAGExecutor       DAG 执行器
├── _db_poll_interval: 10s       DB 轮询间隔
└── _check_interval: 1s          堆检查间隔
```

### 8.2 调度主循环

```
while _running:
  ├── 检查堆中到期的 flow（next_run <= now）
  │   ├── 弹出 flow
  │   ├── 异步触发执行（asyncio.create_task）
  │   └── 计算下次运行时间，重新入堆
  ├── 定期轮询 DB（每 10 秒）
  │   ├── 加载 enabled flows
  │   ├── 移除已禁用/删除的 flows
  │   └── 添加新 flow 或更新版本
  └── sleep(1 秒)
```

**Cron 计算**：使用 croniter 库解析标准 5 字段 cron 表达式，每次执行后计算下次运行时间。

**回溯模式（backfill）**：按交易日列表串行触发多个 FlowRun，每天等待完成再触发下一天。

### 8.3 DAG 执行器

**拓扑排序**：Kahn 算法，返回层列表，每层任务可并行执行，检测循环依赖。

**执行流程**：

```
execute_flow(flow_run, tasks)
  ├── 更新 FlowRun 状态为 RUNNING
  ├── 拓扑排序 → layers
  ├── 逐层执行:
  │   ├── asyncio.gather 并行执行层内所有任务
  │   ├── 检查任务结果
  │   └── 任一失败则中断后续层
  └── 更新最终状态（SUCCESS/FAILED）
```

**任务类型**：sync（数据同步）、etl（ETL 转换）、factor（因子计算）、flow（子流程）

### 8.4 预置流程配置

**daily-sync.yaml**（工作日 18:00 触发）：

```
sync_daily → sync_daily_basic → sync_adj_factor
           → sync_stk_limit → sync_stock_st → sync_suspend_d
                    ↓（全部完成后）
              etl_stock_daily_status
                    ↓
         factor_ma_20 || factor_pe_rank（并行）
```

**weekly-analysis.yaml**（周六 03:00 触发）：

```
sync_stock_basic || sync_daily（并行）
        ↓
factor_ma_5 || factor_rsi_14（并行）
```

### 8.5 任务执行器

三类任务执行函数，均使用 `@tracked_task` 装饰器自动记录执行状态：

| 任务类型 | 执行逻辑 |
|---------|---------|
| Sync | 调用 sync_engine.sync_task()，返回同步行数 |
| ETL | 加载配置 → 确保目标表 → 替换脚本模板变量 → 执行 DolphinDB 脚本 → Upsert 结果 |
| Factor | 调用 FactorComputeService.compute_factor()，返回计算行数 |

**ETL 脚本模板变量**：`{date}`、`{db_ts}`（DolphinDB 连接）、`{db_meta}`（PostgreSQL 连接）

---

## 9. 安全机制

### 9.1 因子代码沙箱（双层防护）

**第一层：AST 静态分析**

```python
class SecurityAnalyzer(ast.NodeVisitor):
    # 检查 import 语句
    # 检查属性访问
    # 检查函数调用
    # 检查变量引用
```

**禁止导入的模块**：
```
os, sys, subprocess, pickle, ctypes, builtins, importlib,
socket, threading, multiprocessing, timeit, gc, inspect
```

**禁止访问的属性**：
```
__dict__, __class__, __bases__, __subclasses__, __mro__,
__globals__, __locals__, __code__, __init__, __new__
```

**禁止调用的函数**：
```
eval, exec, compile, open, file, input, globals, locals,
vars, getattr, setattr, delattr
```

**第二层：运行时沙箱**

仅允许安全 builtins：`abs, all, any, bool, dict, enumerate, filter, float, int, isinstance, len, list, map, max, min, range, round, set, sorted, str, sum, tuple, zip`

执行约束：
- ThreadPoolExecutor + timeout 强制终止（30 秒）
- 内存限制配置（256MB）
- 受限 globals/locals

### 9.2 SQL 安全

- 表名白名单验证（防止 SQL 注入）
- 参数化查询（DolphinDB 客户端）
- 查询结果行数限制（DEFAULT_QUERY_LIMIT=10,000，MAX=1,000,000）

### 9.3 输入验证

- 所有 API 入参通过 Pydantic 模型验证
- Schema 演变验证（任务配置更新时检查字段兼容性）
- 共享表保护（防止删除被多个任务共享的表）

---

## 10. 监控体系

### 10.1 性能监控架构

**装饰器模式**，自动收集指标：

```python
@performance_monitor(track_memory=True)
def compute_factor(data): ...

@async_performance_monitor(track_memory=True)
async def async_query(): ...
```

**PerformanceMetrics 数据类**：

| 字段 | 说明 |
|------|------|
| duration_seconds | 执行时长 |
| memory_start/end/peak/delta_mb | 内存使用 |
| cpu_percent | CPU 使用率 |
| db_queries | 数据库查询数 |
| data_rows | 处理数据行数 |
| data_size_mb | 数据大小 |
| error | 错误信息 |

### 10.2 MetricsCollector 单例

提供统计聚合能力：

| 方法 | 返回 |
|------|------|
| get_statistics(func, window) | count、duration(min/max/mean/p95/p99)、error_rate、qps、throughput |
| get_function_summary() | 所有函数汇总 |
| get_recent_errors(limit) | 最近错误列表 |
| get_resource_usage_summary() | 总执行次数、总时长、总内存、总查询数 |
| export_metrics(filepath) | 导出为 JSON |

### 10.3 健康检查端点

| 端点 | 功能 |
|------|------|
| GET /health | 存活检查（liveness） |
| GET /ready | 就绪检查（检查 PostgreSQL + DolphinDB 连通性） |
| GET /metrics | Prometheus 格式指标 |

---

## 11. 基础设施

### 11.1 Docker 服务配置

| 服务 | 镜像 | 端口 | 健康检查 |
|------|------|------|---------|
| dolphindb | dolphindb/dolphindb:v3.00.5 | 8848 | curl http://localhost:8848/ 每 15s |
| postgres | postgres:16-alpine | 5432 | pg_isready 每 10s |

两个服务均配置 `restart: unless-stopped`，挂载本地数据卷持久化数据。

### 11.2 系统常量

```python
# 交易日
TRADING_DAYS_PER_YEAR = 252

# 因子默认参数
DEFAULT_MA_WINDOW = 20
DEFAULT_RSI_WINDOW = 14
DEFAULT_MACD_FAST/SLOW/SIGNAL = 12/26/9
DEFAULT_BOLLINGER_WINDOW = 20
DEFAULT_ATR_WINDOW = 14

# 数据同步
DEFAULT_START_DATE = "20100101"
DEFAULT_BATCH_SIZE = 5000
MAX_RETRY_ATTEMPTS = 3

# API 限流
DEFAULT_RATE_LIMIT_CALLS = 120  # 次/分钟
```

### 11.3 服务访问地址

| 服务 | 地址 |
|------|------|
| 前端界面 | http://localhost:3000 |
| API 文档 | http://localhost:8000/docs |
| DolphinDB Web UI | http://localhost:8848 |
| PostgreSQL | localhost:5432 |

### 11.4 配置管理

支持两种环境变量命名方式：

```bash
# 扁平命名
TUSHARE_TOKEN=your_token
DOLPHINDB_HOST=localhost

# 嵌套命名（对应 settings.xxx.yyy）
DOLPHINDB__USERNAME=admin
```

---

## 12. 问题与建议

### 12.1 未实现功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| 回测引擎 | ❌ 已删除 | VectorBT 封装、相关服务、DB 表、迁移脚本已全部清除，计划从 0 重构 |
| AutoML | ❌ 未实现 | 无任何业务代码 |
| ML 权重服务 | ❌ 未实现 | 前端 ml.ts 为占位符，后端无路由 |
| 策略 DSL 解析器 | ❌ 未实现 | engine/parser/ 目录不存在 |
| strategy API | ❌ 未实现 | /api/v1/strategy 路由不存在 |

### 12.2 已识别问题

| 优先级 | 问题 | 位置 |
|--------|------|------|
| P1 | 手写类型文件（task.ts、factor.ts、data.ts）与 generated.ts 存在重复定义，需逐步迁移 | frontend/src/types/ |
| P1 | DolphinDB 客户端使用全局线程锁，高并发下可能成为瓶颈 | infrastructure/database/connection.py |
| P2 | 调度器手动运行复用 backfill 接口，缺少单次触发 endpoint（`/flows/{name}/trigger` 前端调用但后端不存在） | app/api/v1/flows.py |
| P2 | 调度器 DB 轮询间隔固定 10s，Flow 配置变更有延迟 | scheduler/core.py |
| P2 | 因子代码沙箱超时 30s 在复杂因子下可能不足 | app/core/sandbox.py |
| P3 | 缺少前端 E2E 测试，仅有后端单元/集成测试 | frontend/ |
| P3 | ETL 脚本直接执行 DolphinDB 脚本，缺少脚本级别的安全审计 | task_executors.py |

### 12.3 架构优势

1. **双数据库职责清晰**：DolphinDB 专注时序数据，PostgreSQL 专注配置管理，各司其职
2. **管道模式灵活**：8 阶段处理管道，每阶段可独立开关，易于扩展新的预处理步骤
3. **泛型服务层**：`TaskService[T]` 统一管理三类任务，减少重复代码
4. **双层沙箱安全**：AST 静态分析 + 运行时受限执行，有效防止恶意代码
5. **类型同步机制**：OpenAPI → generated.ts 自动生成，前后端类型强一致
6. **自研调度器**：最小堆 + Cron + DAG，轻量高效，无 Prefect 等重型依赖
7. **质量标记体系**：位掩码设计，单字段表达多维度数据质量信息
8. **数据库驱动因子**：因子代码存储在 PostgreSQL，动态加载执行，无需重启即可更新
9. **字段命名统一**：DB 列名与 Pydantic 字段名完全一致（无 _json 后缀），消除桥接层

### 12.4 改进建议

1. **连接池化**：DolphinDB 客户端引入连接池，替代单连接 + 全局锁模式
2. **类型统一**：完成手写类型文件向 generated.ts 的迁移，消除类型重复
3. **调度器事件驱动**：DB 轮询改为 PostgreSQL LISTEN/NOTIFY，降低延迟
4. **单次触发 endpoint**：新增 `POST /flows/{name}/trigger` 用于手动单次执行，与 backfill 分离
5. **因子计算并行化**：批量因子计算引入 asyncio 并发，提升吞吐量
6. **前端 E2E 测试**：引入 Playwright 覆盖核心用户流程
7. **指标持久化**：MetricsCollector 当前为内存存储，建议持久化到 PostgreSQL 或 Prometheus

---

## 13. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v2.1.0 | 2026-04-25 | 去除 DB 列名 _json 后缀；删除 @factor 装饰器死代码；删除旧迁移脚本；修复 factor_data_config 错误引用；修复 sync_daily_data KeyError params |
| v2.0.0 | 2026-04-25 | 初始完整架构分析 |

---

*报告生成时间：2026-04-25 | 基于代码静态分析，不含运行时行为验证*
