// ============================================================
// 量化研究系统 DolphinDB 数据库初始化脚本
// 仅创建数据库（dfs://quant）
// 元数据表（维度表）和 TSDB 分区表均由后端应用动态创建：
//   - 元数据表: DolphinDBClient.ensure_meta_tables() 在启动时创建
//   - TSDB 分区表: TableManager.ensure_table() 在首次同步任务时创建
// ============================================================

// 登录（用户名密码由 init_dolphindb.py 通过 session.login 完成，此处做兜底）
try { login("admin", "123456") } catch(ex) { /* 已通过 session 登录 */ }

// ==================== 数据库 dfs://quant ====================
// COMPO 分区: VALUE(trade_date 按月) × HASH(ts_code, 50)
// 引擎: TSDB

if (!existsDatabase("dfs://quant")) {
    // 月份分区范围: 2010.01 ~ 2030.12
    monthRange = 2010.01M..2030.12M
    dbDate = database("", VALUE, monthRange)
    dbCode = database("", HASH, [SYMBOL, 50])
    db = database("dfs://quant", COMPO, [dbDate, dbCode], engine="TSDB")
    print("已创建数据库 dfs://quant")
} else {
    db = database("dfs://quant")
    print("数据库 dfs://quant 已存在，跳过创建")
}

print("========================================")
print("DolphinDB 数据库初始化完成!")
print("  dfs://quant : 数据库已就绪")
print("  元数据表和 TSDB 分区表将由后端应用启动时自动创建")
print("========================================")
