
# 系统架构与状态文档

## 1. System Overview (系统概述)

Quant Research System 是一个全栈量化交易研究平台，专注于因子计算、策略回测和生产部署。系统采用**双数据库架构**（DolphinDB 存储时序数据，PostgreSQL 存储元数据），结合自研调度器实现定时任务和 DAG 工作流执行。

核心功能包括：
- 数据同步（Tushare API）与清洗
- 因子计算与管理（技术指标、自定义因子）
- 因子分析（IC/IR 分析、分组收益）
- 策略可视化设计（React Flow）
- 任务调度与监控
- 回测引擎（基于 Polars 向量化计算）

## 2. Technology Stack & Environment (技术栈与环境)

### 后端技术栈
- **Web 框架**: FastAPI ≥ 0.111.0 (Python 3.11)
- **数据存储**: 
  - DolphinDB ≥ 3.0.0.0 (时序数据库)
  - PostgreSQL (元数据存储)
- **数据处理**:
  - Polars ≥ 1.0.0 (高性能数据处理)
  - Pandas ≥ 2.1.0 (兼容性支持)
  - NumPy ≥ 1.26.0
- **量化计算**:
  - alphalens-reloaded ≥ 0.4.3 (因子分析)
- **任务调度**:
  - asyncpg ≥ 0.29.0 (PostgreSQL 连接池)
  - croniter ≥ 2.0.0 (cron 表达式解析)
  - psycopg2-binary ≥ 2.9.0
- **数据同步**: tushare ≥ 1.4.19 (金融数据 API)
- **其他**:
  - loguru ≥ 0.7.0 (日志)
  - pydantic ≥ 2.7.0 (数据验证)
  - prometheus-client ≥ 0.20.0 (监控)

### 前端技术栈
- **框架**: React 18 + TypeScript
- **UI 库**: Ant Design 6.3.3
- **数据可视化**: 
  - ECharts 5.5.0
  - Highcharts 12.6.0
  - lightweight-charts 5.1.0
- **流程图**: React Flow 11.11.4
- **状态管理**: Zustand 4.5.4
- **HTTP 客户端**: Axios 1.7.2
- **编辑器**: Monaco Editor
- **工具**:
  - dayjs (日期处理)
  - cronstrue (cron 表达式解析)

### 部署与运维
- Docker & Docker Compose
- Nginx (反向代理)
- Uvicorn (ASGI 服务器)

## 3. Architecture Breakdown (架构拆解)

### 3.1 UI & Frontend (前端展示层)

#### 核心页面结构
```
src/pages/
├── MarketCenter.tsx              # 行情中心
├── DataCenter/                   # 数据中心
│   ├── index.tsx                 # 主页面（包含任务面板）
│   ├── SyncTaskDrawer.tsx        # 同步任务配置抽屉
│   ├── ETLTaskDrawer.tsx         # ETL 任务配置抽屉
│   ├── SchedulerPanel.tsx        # 调度器面板
│   └── Modals/                   # 各类模态框
├── FactorCenter/                 # 因子中心
│   ├── index.tsx                 # 主页面（标签页）
│   ├── FactorManageTab.tsx       # 因子管理
│   └── AnalysisPanel.tsx         # 因子分析
├── ConfigCenter/                 # 配置中心
└── UnifiedTaskCenter.tsx         # 统一任务中心
```

#### 状态管理
- **Zustand** 轻量级状态管理
- 核心 Store: `src/store/index.ts`
- 主题切换: `src/store/themeStore.ts`

#### 后端交互模式
- 所有 API 都在 `/api/v1/` 前缀下
- 使用 `openapi-typescript` 自动生成类型定义
- API 调用封装在 `src/api/` 目录下
- 前端开发服务器代理 `/api` 到 `http://localhost:8000`

### 3.2 Backend Services (后端服务层)

#### 核心目录结构
```
backend/
├── app/                          # FastAPI 应用层
│   ├── api/v1/                   # API 路由
│   │   ├── data/                 # 数据相关接口
│   │   ├── factor/               # 因子相关接口
│   │   ├── config/               # 配置管理
│   │   ├── tasks/                # 统一任务管理
│   │   └── flows.py              # 工作流管理
│   ├── core/                     # 核心组件
│   │   ├── config.py             # 配置管理
│   │   ├── logger.py             # 日志
│   │   ├── exceptions.py         # 自定义异常
│   │   ├── sandbox.py            # 因子代码沙箱
│   │   └── utils/                # 工具函数
│   └── services/                 # 业务逻辑层
│       └── factor_service.py     # 因子计算服务
├── engine/                       # 引擎层
│   ├── factor/                   # 因子系统
│   │   ├── registry.py           # 因子注册表
│   │   └── data_config.py        # 数据配置加载
│   ├── analysis/                 # 因子分析
│   └── processor/                # 数据处理管道
├── data_manager/                 # 数据管理层
│   ├── refactored_sync_engine.py # 同步引擎（重构后）
│   ├── sync_components.py        # 同步组件
│   └── collectors/               # 数据采集器
├── scheduler/                    # 自研调度器
│   ├── core.py                   # 调度器核心
│   ├── executor.py               # DAG 执行器
│   ├── models.py                 # 数据模型
│   └── repository.py             # 数据访问层
└── infrastructure/               # 基础设施层
    ├── processor/                # 处理管道
    ├── database/                 # 数据库客户端
    └── monitoring/               # 监控
```

#### API 路由设计
| 路由前缀 | 功能 | 文件位置 |
|---------|------|---------|
| `/api/v1/data/*` | 数据查询和同步操作 | `backend/app/api/v1/data/` |
| `/api/v1/factor/*` | 因子计算和管理 | `backend/app/api/v1/factor/` |
| `/api/v1/flows/*` | 工作流编排 | `backend/app/api/v1/flows.py` |
| `/api/v1/tasks/*` | 统一任务管理 | `backend/app/api/v1/tasks.py` |
| `/api/v1/config/*` | 配置管理 | `backend/app/api/v1/config/` |

#### 核心业务逻辑
1. **因子计算服务** (`FactorComputeService`): 负责因子计算的完整流程
2. **同步引擎** (`RefactoredSyncEngine`): 负责数据同步任务的执行
3. **调度器** (`Scheduler`): 自研调度器，支持 cron 和手动触发
4. **数据访问** (`DolphinDBClient`): 封装 DolphinDB 操作，提供统一接口

### 3.3 Database & Data Persistence (数据层)

#### 双数据库架构
- **DolphinDB** (`dfs://quant`): 时序数据存储，TSDB 引擎优化
- **PostgreSQL**: 元数据和配置存储

#### DolphinDB 表结构

**维度表（元数据表）**:
- `sync_task_config` - 同步任务配置
- `etl_task_config` - ETL 任务配置
- `factor_metadata` - 因子元数据
- `factor_analysis` - 因子分析结果
- `stock_basic` - 股票基础信息
- `trade_cal` - 交易日历
- `sync_log` - 同步任务日志

**时序数据表**:
- `daily_data` - 日线行情数据（OHLCV）
- `daily_basic` - 每日基本指标（PE、PB、换手率等）
- `adj_factor` - 复权因子数据
- `index_daily` - 指数每日数据
- `moneyflow` - 资金流数据
- `factor_values` - 因子计算结果表（核心存储）

**factor_values 表分区策略**:
- 分区类型: **COMPO**（复合分区）
- 第一层: VALUE(trade_date, 月份) - 按月份范围分区
- 第二层: HASH(ts_code, 50) - 股票代码哈希分区（50个桶）
- 分区效果: 查询特定日期数据时，只扫描对应月份分区 + 股票哈希桶，大幅提升查询速度

#### PostgreSQL 表结构
- `factor_configs` - 因子配置（包含因子代码）
- `sync_task_configs` - 同步任务配置
- `etl_task_configs` - ETL 任务配置
- `task_runs` - 任务运行记录
- `flow_runs` - 工作流运行记录

### 3.4 Algorithm & Quant Engine (算法与策略引擎层)

#### 核心量化逻辑

**因子注册表** (`backend/engine/factor/registry.py`):
```python
@dataclass
class FactorDefinition:
    factor_id: str
    description: str
    func: Callable
    depends_on: List[str]
    category: str
    params: Dict[str, Any]
    compute_mode: str  # "incremental" 或 "full"
    storage: StorageConfig
    align_calendar: bool
```

**因子计算流程** (`backend/app/services/factor_service.py`):
1. `discover_factors()` - 从 PostgreSQL 动态加载因子
2. `_resolve_preprocess_options()` - 解析预处理选项
3. `_resolve_dates()` - 确定计算日期范围（支持增量/全量）
4. `pipeline_factory.create_factor_pipeline()` - 创建处理管道
5. `pipeline.execute()` - 执行管道
6. `_save_results()` - 保存计算结果到 factor_values 表

**数据处理管道** (`backend/infrastructure/processor/`):
```
ProcessContext
  ↓
DataLoaderProcessor      # 加载依赖数据
  ↓
AdjustmentProcessor      # 价格复权处理
  ↓
StatusFilterProcessor    # 股票状态过滤（ST、新股）
  ↓
FactorComputeProcessor   # 执行因子计算
  ↓
DateRangeFilterProcessor # 日期范围过滤
  ↓
CalendarAlignProcessor   # 交易日历对齐
  ↓
QualityCheckerProcessor  # 质量检查（空值率、极端值）
  ↓
ResultWriterProcessor    # 结果保存
```

**支持的技术指标因子**:
- 移动平均线: MA, EMA
- 振荡指标: RSI, MACD, KDJ
- 波动率指标: Bollinger Bands, ATR
- 其他: 成交量指标、资金流指标

## 4. Data Flow (核心数据流转)

### 4.1 因子计算流程
```
前端请求计算因子
    ↓
POST /api/v1/factor/compute
    ↓
FactorComputeService.compute_factor()
    ↓
    ├─ 发现因子（registry）
    ├─ 解析预处理配置
    ├─ 确定计算日期范围
    ├─ 创建处理管道
    ├─ 执行管道（各阶段处理）
    │   ├─ 加载数据（DolphinDB）
    │   ├─ 价格复权
    │   ├─ 状态过滤
    │   ├─ 执行因子计算
    │   ├─ 日期对齐
    │   └─ 质量检查
    └─ 保存结果（DolphinDB factor_values 表）
    ↓
返回计算结果给前端
```

### 4.2 数据同步流程
```
前端/调度器触发同步
    ↓
POST /api/v1/data/sync/task/{task_id}
    ↓
SyncEngine.sync_task()
    ├─ SyncConfigManager.get_task()
    └─ SyncTaskExecutor.execute_task()
        ├─ 获取上一次同步时间（SyncLogManager）
        ├─ 确定增量同步范围
        ├─ 调用 Tushare API（TushareAPIClient）
        ├─ 数据清洗和格式转换
        ├─ 创建表（TableManager，如需要）
        ├─ 写入数据（DolphinDB）
        └─ 更新同步日志（SyncLogManager）
    ↓
返回同步结果
```

### 4.3 因子分析流程
```
前端请求因子分析
    ↓
GET /api/v1/factor/analysis?factor_id=xxx&start_date=xxx&end_date=xxx
    ↓
FactorAnalysisService.analyze()
    ├─ 获取因子值（DolphinDB）
    ├─ 获取价格数据（DolphinDB）
    ├─ 计算 IC/IR（信息系数）
    ├─ 分组收益分析（分位数）
    ├─ 计算 Sharpe 比率、最大回撤等
    └─ 质量检查（空值率、极端值）
    ↓
返回分析结果给前端
```

## 5. Vulnerability & Tech Debt Analysis (风险与技术债分析)

### 5.1 架构缺陷
- **单点故障**: 调度器是单点运行，无冗余备份
- **缺少分布式计算支持**: 因子计算目前是单进程，不支持分布式计算
- **因子代码沙箱不完善**: 使用 exec 执行动态加载的因子代码，虽然有基本的安全检查，但仍有风险
- **同步任务串行执行**: 同步任务按顺序执行，未充分利用资源

### 5.2 代码规范问题
- 部分模块命名不一致（如 `factor_service` vs `factors`）
- 缺乏详细的 API 文档注释
- 部分工具函数和配置分散在多个文件中
- 测试覆盖不足，特别是集成测试

### 5.3 量化特有风险
- **浮点数精度问题**: 使用 Python 原生浮点数，未做高精度处理
- **时序数据对齐**: 交易日历对齐逻辑复杂，可能存在边界条件问题
- **数据质量问题**: 因子计算前的质量检查不够严格
- **复权处理**: 前复权/后复权逻辑在不同场景下的一致性

### 5.4 扩展性瓶颈
- **多账户支持不足**: 架构设计未考虑多账户体系
- **高频行情接入困难**: 目前架构针对日线级数据优化，不适合高频数据处理
- **因子依赖管理**: 因子间依赖关系管理复杂，缺乏可视化
- **计算资源分配**: 因子计算资源分配缺乏弹性

## 6. 🤖 AI Context Anchor (用于未来的 LLM 交互)

### 系统核心原则
1. **数据与计算分离**: DolphinDB 存储时序数据，计算在内存中完成
2. **因子代码沙箱**: 所有用户代码必须通过安全检查后执行
3. **增量计算优先**: 支持增量计算，避免重复计算
4. **统一任务接口**: 所有任务通过 /api/v1/tasks/* 接口管理
5. **配置驱动**: 系统行为主要通过配置而不是代码修改

### 代码风格约定
- **Python**: 
  - 使用 PEP 8 规范
  - 类型注解覆盖率 ≥ 80%
  - 函数长度 ≤ 50 行，文件长度 ≤ 800 行
  - 禁止使用 print()，使用 loguru 日志
- **TypeScript**:
  - 使用 eslint + Prettier
  - 接口类型定义在 src/types/
  - 状态管理使用 Zustand
- **API**:
  - 所有 API 在 /api/v1/ 前缀下
  - 使用 OpenAPI 文档
  - 响应格式: { success: boolean, data?: any, error?: string }

### 新增模块规范

#### 新增 API 接口
1. 放在对应的模块目录下: `backend/app/api/v1/[module]/`
2. 使用 Pydantic 模型验证请求/响应
3. 统一使用 HTTP 状态码
4. 抛出自定义异常（继承自 QuantException）

#### 新增因子类型
1. 在 PostgreSQL factor_configs 表中添加配置
2. 编写因子计算代码（支持 Polars 向量化）
3. 因子代码必须包含 compute 或 main 函数
4. 配置依赖的数据表和参数

#### 新增同步任务
1. 在 sync_task_config 表中添加任务配置
2. 实现对应的同步采集器（data_manager/collectors/）
3. 配置同步类型（增量/全量）和日期字段

#### 新增处理器
1. 继承自 IProcessor 接口
2. 实现 process() 方法
3. 配置在 pipeline_factory 中
4. 确保线程安全

### 部署规范
- 所有服务通过 Docker Compose 启动
- 环境变量在 .env 文件中配置
- 启动脚本: `start.sh`, `stop.sh`, `check_status.sh`
- 日志位置: `logs/backend.log`, `logs/frontend.log`

---

## 文档更新记录

**2026-04-26** - 基于实际代码分析的初始版本
- 验证了双数据库架构
- 明确了因子计算流程
- 描述了实际使用的分区策略
- 修正了之前文档中的不准确信息（如 VectorBT 实际未使用）
